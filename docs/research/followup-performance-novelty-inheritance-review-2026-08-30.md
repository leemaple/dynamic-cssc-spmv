# Follow-up performance study: bounded novelty-inheritance review

> **State:** Stage-1 review draft; non-authorizing. This note decides only
> whether the already bounded Route A combination-novelty conclusion can be
> inherited by the post-failure follow-up study. It is not experimental
> evidence, a patent search, or a global novelty opinion.

## Exact ancestry

- Follow-up Stage-1 base commit:
  `4f328afc079b328c31f2e0790cb65cdf96fcc1d7`.
- Base tree:
  `ab48bd66a2a8ae99da17c6cd960b71fffffb71bc`.
- Predecessor machine plan:
  `config/route-a-publication-plan.json`, SHA-256
  `ce09c1c9c82032ba8439188ce20d4cd8d6310a386efbe2d436595fd779b7268c`.
- Predecessor preregistration:
  `docs/paper/publication-preregistration-route-a.md`, SHA-256
  `6b53a73c6973a4be53d195f5d9407e7e023ae3a5617bce57b4a40a7033a32f79`.
- Bounded primary-source novelty review:
  `docs/research/route-a-complete-combination-novelty-review-2026-08-28.md`,
  SHA-256
  `6030bec34d194aeaa59b813b06c34c9f5a901ef0e1fa8bafadf1b1079a080ba5`.
- Predecessor comparison baseline after the five registered seed replacements,
  in the canonical materialization defined by
  `config/followup-performance-study.json`: SHA-256
  `0d307169356a50cc75f6ad7ba1c018321c0693e185cde7f5f2e7fef472da8e0e`.

## Frozen contribution vector

The follow-up retains exactly the predecessor's four-condition combination:

1. **C1 — version-bound mutable CSSC publication.** A publication window binds
   the logical matrix version, physical component set, global-column metadata,
   private reconstruction plan, prepared query, and associated evidence
   identities.
2. **C2 — global-column query reorganization.** Reorganization is defined by
   true global column identities, not by a physical ciphertext-lane ordinal.
3. **C3 — private RowMap-aware multi-component reconstruction.** The client-side
   reconstruction covers overlapping contributors, disjoint row blocks, and
   implicit zeros while preserving the stated functional result modulo the
   plaintext modulus.
4. **C4 — overlap-scoped canceling masks with durable no-reuse identity.** F1-M
   masks cancel only inside a frozen overlap contributor group, while the
   reserve-before-sample ledger prevents reuse of the complete frozen identity.

The follow-up changes none of C1--C4. Its five registered scientific-value
changes are fresh seeds only. Inherited inner scientific schema identities and
canonical domains remain byte-exact; the new outer envelope, workflow,
artifact, Behavior Set, anchor, and controller identities are evidence-lineage
separation, not a fifth technical contribution.

## Effect of the outcome-informed repair

Commits `e204bb90fcfce0b2e9f3082fc2849c2de41e3b4b` and
`5b5d1468bfe3cac324819747388a2c897101f2bb` remove redundant repeated lifecycle
validation while preserving deep canonical validation, direct-oracle equality,
ledger closure, and independent replay. The repair was selected after the
closed qualification exposed a bottleneck. It is therefore disclosed as
outcome-informed engineering provenance.

The repair is not asserted to be:

- a new cryptographic primitive or protocol contribution;
- a novel asymptotic algorithm;
- evidence of a before/after speedup;
- proof that the predecessor experiment would have passed; or
- support for any performance, security, first, only, or global claim.

Consequently, it does not expand the prior-art search question. Any statement
about its runtime effect remains forbidden unless a separately designed study
had preregistered such an estimand; this follow-up did not.

## Inheritance decision

The predecessor review's narrow result is inherited:

- **PASS:** within its documented bounded primary-source collision search, no
  single reviewed prior work was found to combine C1--C4.
- **HOLD:** every `first`, `only`, `earliest`, exhaustive, patent, global
  novelty, or formal-security statement.

This inheritance is valid only while the exact C1--C4 vector and contribution
sentence remain unchanged. It must reopen before interpretation or submission
if a previously unavailable directly relevant source becomes public, including
the SparseE full text, or if Stage 2 changes any contribution-bearing behavior.
The reopening rule cannot cancel or reinterpret an already completed exact run;
it pauses additional dispatch, aggregation, claims, and submission as specified
by the inherited plan.

## Gate disposition

Local disposition is **PASS for bounded inheritance, with no new novelty
claim**. The note remains non-authorizing until the exact Stage-1 file set has
received the configured same-packet independent review and no P0/P1 remains.

# Validation-scaling claim ledger

Study: `dynamic-cssc-validation-scaling-2026-09-01`.

Status: **Stage-0 v2 amendment candidate; no implementation or result exists.**

| Claim ID | Candidate claim | Required evidence | Current state |
|---|---|---|---|
| VS-C1 | The complete 54-cell registered matrix preserved the frozen canonical producer/replay semantic projection. | Exact v2 source tag; one attempt-1 workflow; three one-day private producer artifacts, three 90-day redacted replay artifacts, aggregate guard; byte equality and SHA-256 equality for the closed projection that excludes only `measurements` | HOLD — no dispatch |
| VS-C2 | Every measured cell used between one and two real `compile_query` calls per query. | Cell query counts and complete call counts from the shared wrapper installed in both frozen live bindings; restoration/fault tests; aggregate recomputation | HOLD — no dispatch |
| VS-C3 | Current-source validation-bearing lifecycle timing scales as descriptively reported on the pinned hosted-runner environment and frozen counterbalanced order. | All 54 operation wall/process observations; frozen integer median/min/max table and exact-rational OLS fit; six dependency-job provider intervals read post-terminal by aggregate; complete two-file seed-artifact inventory; extrapolated-intercept label | HOLD — no dispatch |
| VS-C4 | Producer and independent replay have the separately reported registered stage costs. | Distinct provider jobs and role-specific-retention artifacts; no same-process substitution; producer transition/result-assembly fields, replay elapsed field, producer-cell archive fields, and the supporting per-seed public-operation-through-payload-return interval recorded only in `execution-receipt.json` and excluded from every cell row and fit | HOLD — no dispatch |

The following claims are permanently excluded from this study: a strategy
winner or superiority ranking; reopening either terminal performance lineage;
qualification or campaign GO; historical-source speedup; native OpenFHE
performance; deployment performance; formal security; and general correctness.

Historical pilot values and predecessor q1/q2 timing are context only. They may
not enter any VS-C1--VS-C4 result, statistic, fit, plot, or table cell.

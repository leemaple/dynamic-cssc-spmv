# Validation-scaling claim ledger

Study: `dynamic-cssc-validation-scaling-2026-09-01`.

Status: **preregistered candidate; no implementation or result exists.**

| Claim ID | Candidate claim | Required evidence | Current state |
|---|---|---|---|
| VS-C1 | The complete 54-cell registered matrix preserved the frozen canonical producer/replay semantic projection. | Exact source tag; one attempt-1 workflow; all producer/replay artifacts; aggregate guard; byte equality and SHA-256 equality for the closed projection that excludes only `measurements` | HOLD — no dispatch |
| VS-C2 | Every measured cell used between one and two real `compile_query` calls per query. | Cell query counts and instrumented compile counts from both roles; aggregate recomputation | HOLD — no dispatch |
| VS-C3 | Current-source validation-bearing lifecycle timing scales as descriptively reported on the pinned hosted-runner environment and frozen counterbalanced order. | All 54 operation wall/process observations; frozen median/min/max table and OLS fit; complete provider inventory; extrapolated-intercept label | HOLD — no dispatch |
| VS-C4 | Producer and independent replay have the separately reported registered stage costs. | Distinct provider jobs and artifacts; no same-process substitution; producer transition/result-assembly fields, replay elapsed field, producer-cell archive fields, and per-seed package fields under their exact registered scopes | HOLD — no dispatch |

The following claims are permanently excluded from this study: a strategy
winner or superiority ranking; reopening either terminal performance lineage;
qualification or campaign GO; historical-source speedup; native OpenFHE
performance; deployment performance; formal security; and general correctness.

Historical pilot values and predecessor q1/q2 timing are context only. They may
not enter any VS-C1--VS-C4 result, statistic, fit, plot, or table cell.

# Data policy

Only small synthetic fixtures are stored in Git. Real temporal datasets must be downloaded by a provenance-recording script or supplied through a release asset. Every dataset record must include source, license, checksum, preprocessing command, vertex-universe rule, T1/T2 event semantics, and warm-up/tuning/held-out boundaries.

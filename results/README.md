# Results

Generated results are not committed by default. GitHub Actions uploads them as immutable workflow artifacts. Only reviewed, provenance-complete summaries should later be committed under a versioned experiment directory.
